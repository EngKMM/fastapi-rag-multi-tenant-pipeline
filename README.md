# fastapi-rag-multi-tenant-pipeline
In this project I built a Retrieval-Augmented Generation (RAG) API from scratch. I wanted to see exactly how enterprise engineers connect LLMs to private data under the hood. Instead of relying on paid cloud APIs, this entire pipeline runs locally on my machine taking custom data, chunking it into a vector database, and generating grounded answers.
